name: PIPE-LINE

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      action:
        description: 'What to run'
        type: choice
        required: true
        default: 'plan'
        options:
          - plan
          - apply
          - destroy
jobs:
  terraform-plan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: teraform
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1
      - run: terraform init
      - run: terraform validate
      - run: terraform plan

  terraform-apply:
    needs: terraform-plan 
    runs-on: ubuntu-latest
    environment: production
    defaults:
      run:
        working-directory: teraform
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

  docker-build-deploy:
     needs: terraform-apply
     runs-on: ubuntu-latest
     steps:
      - uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Build Docker image
        run: |
          docker build -t "${var.project_name}-${var.environment}":${{ github.sha }}-latest .

      - name: Tag Docker image
        run: |
          docker tag "${var.project_name}-${var.environment}":${{ github.sha }}-latest \
          646362344732.dkr.ecr.us-east-1.amazonaws.com/"${var.project_name}-${var.environment}":${{ github.sha }}-latest

      - name: Push Docker image
        run: |
          docker push \
          646362344732.dkr.ecr.us-east-1.amazonaws.com/"${var.project_name}-${var.environment}":${{ github.sha }}-latest
  
